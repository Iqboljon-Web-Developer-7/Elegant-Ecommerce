export interface slideType {
  images: {
    asset: { _ref: string };
  };
}
export interface collectionType {
  image: { asset: { _ref: string } };
  title: string;
  urlName: string;
  url: string;
}
